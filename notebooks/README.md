# Notebooks

Example notebooks will be added as the public API settles. For now, use `examples/basic_usage.py` or load the extension in a notebook:

```python
%load_ext trainlens
%explain_training
```
